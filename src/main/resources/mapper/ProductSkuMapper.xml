<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ProductSkuMapper">

    <resultMap id="BaseResultMap" type="com.example.model.entity.ProductSku">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="productId" column="product_id" jdbcType="BIGINT"/>
            <result property="specValues" column="spec_values" jdbcType="VARCHAR"/>
            <result property="price" column="price" jdbcType="DECIMAL"/>
            <result property="stock" column="stock" jdbcType="INTEGER"/>
            <result property="sales" column="sales" jdbcType="INTEGER"/>
            <result property="mainImage" column="main_image" jdbcType="VARCHAR"/>
            <result property="status" column="status" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,product_id,spec_values,
        price,stock,sales,
        main_image,status,create_time,
        update_time
    </sql>

    <!-- 批量更新SKU状态 -->
    <update id="updateSkuStatusBatch">
        UPDATE product_sku
        SET status = #{status}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 价格/库存联合查询 -->
    <select id="selectByPriceAndStock" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_sku
        WHERE status = 1
        <if test="minPrice != null">
            AND price >= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND price &lt;= #{maxPrice}
        </if>
        <if test="hasStock != null and hasStock">
            AND stock > 0
        </if>
        ORDER BY price
    </select>

    <!-- 销量排行榜生成 -->
    <select id="selectSalesRank" resultType="map">
        SELECT 
            sku.id,
            sku.spec_values,
            sku.sales,
            p.name AS product_name
        FROM product_sku sku
        INNER JOIN products p ON sku.product_id = p.id
        WHERE sku.status = 1 AND p.status = 1
        ORDER BY sku.sales DESC
        LIMIT #{limit}
    </select>

    <!-- 更新SKU主图默认值 -->
    <update id="updateMainImageDefault">
        UPDATE product_sku
        SET main_image = 
            CASE 
                WHEN #{defaultImage} IS NOT NULL AND main_image IS NULL 
                THEN #{defaultImage} 
                ELSE main_image 
            END
        WHERE product_id = #{productId}
    </update>
</mapper>
