<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserCouponMapper">

    <resultMap id="BaseResultMap" type="com.example.model.entity.UserCoupon">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="couponId" column="coupon_id" jdbcType="BIGINT"/>
        <result property="status" column="status" jdbcType="TINYINT"/>
        <result property="orderId" column="order_id" jdbcType="BIGINT"/>
        <result property="getTime" column="get_time" jdbcType="TIMESTAMP"/>
        <result property="useTime" column="use_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,coupon_id,
        status,order_id,get_time,
        use_time,create_time,update_time
    </sql>

    <!-- 根据用户ID查询优惠券 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        WHERE user_id = #{userId}
    </select>

    <!-- 分页查询用户优惠券 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID查询用户优惠券 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_coupon
        WHERE id = #{id}
    </select>

    <!-- 插入新用户优惠券 -->
    <insert id="insertUserCoupon" parameterType="com.example.model.entity.UserCoupon">
        INSERT INTO user_coupon (user_id, coupon_id, status, order_id, get_time, use_time, create_time, update_time)
        VALUES (#{userId}, #{couponId}, #{status}, #{orderId}, #{getTime}, #{useTime}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新用户优惠券信息 -->
    <update id="updateUserCoupon" parameterType="com.example.model.entity.UserCoupon">
        UPDATE user_coupon
        SET user_id = #{userId},
            coupon_id = #{couponId},
            status = #{status},
            order_id = #{orderId},
            get_time = #{getTime},
            use_time = #{useTime},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除用户优惠券 -->
    <delete id="deleteUserCoupon">
        DELETE FROM user_coupon WHERE id = #{id}
    </delete>
</mapper>
