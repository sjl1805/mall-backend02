<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.example.model.entity.Category">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="parentId" column="parent_id" jdbcType="BIGINT"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="icon" column="icon" jdbcType="VARCHAR"/>
            <result property="level" column="level" jdbcType="TINYINT"/>
            <result property="sort" column="sort" jdbcType="INTEGER"/>
            <result property="status" column="status" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,parent_id,name,
        icon,level,sort,
        status,create_time,update_time
    </sql>

    <!-- 递归查询子分类 -->
    <select id="selectChildrenRecursive" resultMap="BaseResultMap">
        WITH RECURSIVE cte AS (
            SELECT * FROM category WHERE id = #{categoryId}
            UNION ALL
            SELECT c.* FROM category c
            INNER JOIN cte ON c.parent_id = cte.id
        )
        SELECT <include refid="Base_Column_List"/> FROM cte
    </select>

    <!-- 同级分类排序调整 -->
    <update id="updateSortByParent">
        UPDATE category
        SET sort = CASE id
            <foreach collection="sortedIds" item="id" index="index">
                WHEN #{id} THEN #{index}
            </foreach>
        END
        WHERE parent_id = #{parentId}
    </update>

    <!-- 分类状态批量修改 -->
    <update id="updateStatusBatch">
        UPDATE category
        SET status = #{status}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 带层级的分类树查询 -->
    <select id="selectCategoryTree" resultMap="BaseResultMap">
        SELECT 
            <include refid="Base_Column_List"/>,
            (SELECT COUNT(*) FROM category c2 WHERE c2.parent_id = c1.id) AS child_count
        FROM category c1
        ORDER BY parent_id, sort
    </select>

    <!-- 分类商品数量统计 -->
    <select id="selectCategoryWithProductCount" resultType="map">
        SELECT 
            c.id,
            c.name,
            COUNT(p.id) AS product_count
        FROM category c
        LEFT JOIN products p ON c.id = p.category_id
        WHERE c.status = 1
        GROUP BY c.id
    </select>
</mapper>
