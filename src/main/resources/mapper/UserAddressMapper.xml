<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserAddressMapper">

    <resultMap id="BaseResultMap" type="com.example.model.entity.UserAddress">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="isDefault" column="is_default" jdbcType="TINYINT"/>
        <result property="isDefaultTrue" column="is_default_true" jdbcType="TINYINT"/>
        <result property="receiverName" column="receiver_name" jdbcType="VARCHAR"/>
        <result property="receiverPhone" column="receiver_phone" jdbcType="VARCHAR"/>
        <result property="province" column="province" jdbcType="VARCHAR"/>
        <result property="city" column="city" jdbcType="VARCHAR"/>
        <result property="district" column="district" jdbcType="VARCHAR"/>
        <result property="detailAddress" column="detail_address" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,is_default,
        is_default_true,receiver_name,receiver_phone,
        province,city,district,
        detail_address,create_time,update_time
    </sql>

    <!-- 根据用户ID查询收货地址 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        WHERE user_id = #{userId}
    </select>

    <!-- 分页查询用户收货地址 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID查询用户收货地址 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_address
        WHERE id = #{id}
    </select>

    <!-- 插入新用户收货地址 -->
    <insert id="insertUserAddress" parameterType="com.example.model.entity.UserAddress">
        INSERT INTO user_address (user_id, is_default, receiver_name, receiver_phone, province, city, district,
                                  detail_address, create_time, update_time)
        VALUES (#{userId}, #{isDefault}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district},
                #{detailAddress}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新用户收货地址信息 -->
    <update id="updateUserAddress" parameterType="com.example.model.entity.UserAddress">
        UPDATE user_address
        SET user_id        = #{userId},
            is_default     = #{isDefault},
            receiver_name  = #{receiverName},
            receiver_phone = #{receiverPhone},
            province       = #{province},
            city           = #{city},
            district       = #{district},
            detail_address = #{detailAddress},
            update_time    = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除用户收货地址 -->
    <delete id="deleteUserAddress">
        DELETE
        FROM user_address
        WHERE id = #{id}
    </delete>
</mapper>
