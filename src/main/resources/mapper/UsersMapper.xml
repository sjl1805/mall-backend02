<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UsersMapper">
    <resultMap id="BaseResultMap" type="com.example.model.entity.Users">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="gender" property="gender" jdbcType="TINYINT"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="role" property="role" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,nickname,phone,email,avatar,gender,status,role,create_time,update_time
    </sql>

    <!-- 用户注册 -->
    <insert id="insertUser" parameterType="com.example.model.entity.Users" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users
        (username, password, nickname, phone, email, avatar, gender, status, role)
        VALUES
        (#{username}, #{password}, #{nickname}, #{phone}, #{email}, #{avatar}, #{gender}, #{status}, #{role})
    </insert>

    <!-- 登录验证（支持用户名/手机/邮箱登录） -->
    <select id="selectByLoginId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE 
        <choose>
            <when test="loginId.contains('@')">
                email = #{loginId}
            </when>
            <when test="loginId.matches('^1[3-9]\\d{9}$')">
                phone = #{loginId}
            </when>
            <otherwise>
                username = #{loginId}
            </otherwise>
        </choose>
        AND status = 1
    </select>

    <!-- 更新用户信息（动态更新字段） -->
    <update id="updateUserInfo" parameterType="com.example.model.entity.Users">
        UPDATE users
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="password != null">password = #{password},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 用户状态管理 -->
    <update id="updateUserStatus">
        UPDATE users
        SET status = #{status}
        WHERE id = #{userId}
    </update>

</mapper> 